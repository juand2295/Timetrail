import { Text, StyleSheet, View} from 'react-native'

const TrackCreate = () => {
    return (
        <View>
            <Text style={styles.titles}>Track Create</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    titles: {
        fontSize:30
    }
});

export default TrackCreate;